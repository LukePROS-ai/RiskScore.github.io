# Leave unset for explicitly labelled development demonstrations.
# Set to your public read-only endpoint, preferably a same-origin backend proxy.
# VITE variables are embedded in the browser build: NEVER put credentials here.
VITE_SENSOR_API_URL=
